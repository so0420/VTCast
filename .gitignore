# Rust
target/
**/*.rs.bk
Cargo.lock.tmp

# OS
Thumbs.db
.DS_Store
desktop.ini

# Editor
.idea/
.vscode/
*.swp
*.swo

# Vendored third-party SDKs (fetched on demand)
vendor/

# PoC pipeline intermediates (regenerable)
poc1/packed.png
poc1/packed.mp4
poc1/packed_decoded.png
poc1/reconstructed.png
poc1/reconstructed_preview.png

# Captured frames may contain the user's character — never commit
poc2_spout/captured.png
poc2_spout/captured_preview.png
**/captured*.png

# Node (for future receiver dev tooling)
node_modules/
dist/
.env
.env.local

# Tauri auto-generated schemas (regenerated by tauri-build on each build)
crates/sender-app/gen/

# Release artifacts staged for distribution — built fresh from CI, not committed
dist/*.exe
